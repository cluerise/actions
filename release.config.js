import { createReleaseConfig } from '@cluenar/tools/dist/configs/release.config.js';

export default createReleaseConfig({
  host: 'https://github.com',
  owner: 'cluenar',
  repository: 'actions'
});
